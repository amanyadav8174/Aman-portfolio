/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['img.icons8.com'],
}};

export default nextConfig;


// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     reactStrictMode: true,
//     images: {
//       domains: ['img.icons8.com'], // Add allowed external image domains here
//     },
//   };
  
//   module.exports = nextConfig;
  